const { fetchFinancialMetrics } = require('./finnhub');

async function test() {
  const isINR = true;
  // We can write a quick custom scraper run or log inside our test
  // Let's just edit test_live_fetch.ts to print the raw screener data keys
}
